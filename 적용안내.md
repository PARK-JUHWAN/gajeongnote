# 출처 표기 반영 — 적용 안내

## 1. 덮어쓸 파일

| 받은 파일 | 넣을 곳 |
|---|---|
| `content.json` | `assets\content.json` |
| `main.dart` | `lib\main.dart` |
| `content_repo.dart` | `lib\data\content_repo.dart` |
| `settings_screen.dart` | `lib\screens\settings_screen.dart` |
| `sources.dart` | `lib\models\sources.dart` (신규) |
| `sources_screen.dart` | `lib\screens\sources_screen.dart` (신규) |
| `license_screen.dart` | `lib\screens\license_screen.dart` (신규) |

## 2. `lib\state\app_state.dart` 직접 수정 — 3곳

**① import 추가** (import 묶음 끝)

```dart
import '../models/sources.dart';
```

**② 필드 추가** (`chapters` 선언 아래)

```dart
  Sources sources = Sources.empty;
```

**③ `init()` 안에서 `chapters = await ContentRepo.load();` 를 교체**

```dart
      final loaded = await ContentRepo.load();
      chapters = loaded.$1;
      sources = loaded.$2;
```

## 3. `pubspec.yaml` — assets 한 줄 추가

라이선스 화면이 Pretendard OFL 전문을 읽는다.

```yaml
  assets:
    - assets/content.json
    - fonts/PRETENDARD-LICENSE.txt
```

## 4. 실행

```powershell
flutter pub get
dart analyze
flutter run -d chrome
```

## 5. 확인할 것

- 설정 → **근거 자료** → 법령 / 정부 발간 자료 / 간호 실무지침 3묶음, 14개 항목
- 설정 → **오픈소스 라이선스** → 패키지 목록, 항목을 누르면 전문이 펼쳐짐. Pretendard 포함
- 아무 챕터 → 가족 트랙 맨 아래에 **「이 내용은 법령과 간호 실무지침을 근거로…」** 한 줄

## 6. 심사 노트에 한 줄 추가

```
모든 내용은 대한민국 법령, 보건복지부 발간 자료, 병원간호사회 근거기반
임상간호실무지침을 근거로 재작성한 것이며, 원문을 복제하지 않았습니다.
근거 목록은 앱 내 설정 → 근거 자료에서 확인할 수 있습니다.
```

## 7. 주의

- `showLicensePage()`를 쓰지 않았다. 내부에서 `MaterialPageRoute`와 `Scaffold`를 쓰는데
  이 앱에는 `Theme`가 없어 터진다. `LicenseRegistry`를 직접 읽는 화면을 만들었다.
- 출처 화면의 URL은 **도메인 텍스트만** 표기하고 열리지 않는다.
  `url_launcher`를 넣으면 「네트워크 통신을 하지 않는다」는 심사 노트 문장이 약해진다.
